 <% if !@portfolio.blank? %>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h5><%= @portfolio.title%></h5>
      <p><%= @portfolio.overview %></p>
      <iframe src="<%= @portfolio.file_url %>" width="800" height="700" style ="border: none;"></iframe>
    </div>
  </div>
</div>
<% end %>