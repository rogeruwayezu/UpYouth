<h1>Jobs Available</h1>

<% @posts.each do |post| %>
  <p><%= link_to post.title, "/posts/#{post.id}"%></p>
  <p><%= post.description%></p>
<% end %>
  <%= button_to "Profile", "/profiles/#{@profile.id}", class: "btn btn-success", method: :get %>

